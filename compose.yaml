services:
  site:
    image: squidfunk/mkdocs-material:9.7.7
    working_dir: /docs
    command: serve --dev-addr=0.0.0.0:8000 --strict
    ports:
      - "${SWINGBIG_PORT:-8001}:8000"
    volumes:
      - .:/docs
